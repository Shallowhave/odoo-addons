<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- 质量控制点表单视图扩展 -->
    <record id="quality_point_view_form_inherit_rfid" model="ir.ui.view">
        <field name="name">quality.point.form.inherit.rfid</field>
        <field name="model">quality.point</field>
        <field name="inherit_id" ref="quality.quality_point_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='team_id']" position="after">
                <field name="test_type" invisible="1"/>
                <field name="rfid_device_required"
                       invisible="test_type != 'rfid_label'"/>
                <field name="rfid_device_id"
                       invisible="test_type != 'rfid_write' and (test_type != 'rfid_label' or not rfid_device_required)"
                       string="RFID 设备"
                       placeholder="选择RFID设备"/>
            </xpath>
        </field>
    </record>
</odoo>
