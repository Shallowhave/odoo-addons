<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rfid_device_config_form_view" model="ir.ui.view">
        <field name="name">rfid.device.config.form</field>
        <field name="model">rfid.device.config</field>
        <field name="arch" type="xml">
            <form string="RFID 设备配置">
                <header>
                    <button name="action_test_connection" type="object"
                            string="测试连接" class="oe_highlight"/>
                    <button name="action_write_test_tag" type="object"
                            string="测试写入" invisible="validation_state != 'validated'"/>
                    <button name="action_read_test_tag" type="object"
                            string="测试读取" invisible="validation_state != 'validated'"/>
                    <field name="validation_state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_write_logs" class="oe_stat_button"
                                type="object" icon="fa-edit">
                            <field name="write_count" widget="statinfo" string="写入"/>
                        </button>
                        <button name="action_view_read_logs" class="oe_stat_button"
                                type="object" icon="fa-search">
                            <field name="read_count" widget="statinfo" string="读取"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="已禁用" bg_color="bg-danger"
                            invisible="active"/>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="设备名称..." required="1"/></h1>
                    </div>
                    <group>
                        <group string="基本信息">
                            <field name="active"/>
                            <field name="sequence"/>
                            <field name="company_id"/>
                            <field name="device_type"/>
                            <field name="migration_required" readonly="1"/>
                        </group>
                        <group string="Adapter 配置" invisible="device_type != 'si120x1'">
                            <field name="adapter_device_id" required="device_type == 'si120x1'"/>
                            <field name="protocol_family"/>
                        </group>
                        <group string="连接参数" invisible="device_type == 'si120x1'">
                            <!-- Legacy direct-connection parameters -->
                            <field name="connection_string"/>
                            <field name="ip_address"/>
                            <field name="port"/>
                            <field name="baudrate"/>
                            <field name="timeout"/>
                        </group>
                    </group>
                    <group>
                        <group string="硬件能力" invisible="device_type != 'si120x1'">
                            <field name="supports_epc"/>
                            <field name="supports_tid"/>
                            <field name="supports_user_read"/>
                            <field name="supports_user_write"/>
                            <field name="antenna_count"/>
                            <field name="region"/>
                        </group>
                        <group string="版本信息" invisible="device_type != 'si120x1'">
                            <field name="firmware_version"/>
                            <field name="hardware_version"/>
                            <field name="module_version"/>
                        </group>
                    </group>
                    <group>
                        <group string="高级配置">
                            <field name="auto_connect" invisible="device_type == 'si120x1'"/>
                            <field name="retry_times" invisible="device_type == 'si120x1'"/>
                        </group>
                        <group string="状态信息">
                            <field name="connection_status" readonly="1"/>
                            <field name="last_connected" readonly="1"/>
                            <field name="last_connection_test_at" readonly="1" invisible="device_type != 'si120x1'"/>
                            <field name="last_successful_operation_at" readonly="1" invisible="device_type != 'si120x1'"/>
                            <field name="last_device_code" readonly="1" invisible="device_type != 'si120x1'"/>
                            <field name="error_message" readonly="1"
                                   invisible="not error_message"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="备注" name="notes">
                            <field name="notes" placeholder="设备备注信息..."/>
                        </page>
                        <page string="使用说明" name="help">
                            <div class="alert alert-info" role="alert">
                                <h4>SI120X1 集成状态</h4>
                                <p>
                                    仅已验证且属于当前公司的 SI120X1 可用于硬件操作。
                                    配置 <b>Adapter 设备 ID</b> 并确认协议族。
                                </p>
                            </div>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="rfid_device_config_tree_view" model="ir.ui.view">
        <field name="name">rfid.device.config.tree</field>
        <field name="model">rfid.device.config</field>
        <field name="arch" type="xml">
            <list string="RFID 设备">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="company_id"/>
                <field name="device_type"/>
                <field name="adapter_device_id" optional="show"/>
                <field name="validation_state" widget="badge"/>
                <field name="connection_status" widget="badge"
                       decoration-success="connection_status == 'connected'"
                       decoration-danger="connection_status == 'error'"
                       decoration-muted="connection_status == 'disconnected'"/>
                <field name="write_count" optional="hide"/>
                <field name="read_count" optional="hide"/>
                <field name="last_connection_test_at" optional="show"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="rfid_device_config_search_view" model="ir.ui.view">
        <field name="name">rfid.device.config.search</field>
        <field name="model">rfid.device.config</field>
        <field name="arch" type="xml">
            <search string="RFID 设备">
                <field name="name"/>
                <field name="device_type"/>
                <field name="adapter_device_id"/>
                <field name="company_id"/>
                <filter string="启用" name="active" domain="[('active', '=', True)]"/>
                <filter string="已验证" name="validated" domain="[('validation_state', '=', 'validated')]"/>
                <filter string="连接错误" name="error" domain="[('connection_status', '=', 'error')]"/>
                <separator/>
                <filter string="已归档" name="inactive" domain="[('active', '=', False)]"/>
                <group expand="0" string="分组">
                    <filter string="设备类型" name="group_by_type" context="{'group_by': 'device_type'}"/>
                    <filter string="验证状态" name="group_by_validation" context="{'group_by': 'validation_state'}"/>
                    <filter string="公司" name="group_by_company" context="{'group_by': 'company_id'}"
                            groups="base.group_multi_company"/>
                </group>
            </search>
        </field>
    </record>

    <record id="rfid_device_config_action" model="ir.actions.act_window">
        <field name="name">RFID 设备配置</field>
        <field name="res_model">rfid.device.config</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">创建第一个 RFID 设备配置</p>
            <p>配置 SI120X1 设备，指定其对应的 Adapter 设备 ID。</p>
        </field>
    </record>

    <menuitem id="menu_rfid_device_config" name="设备配置"
              parent="xq_rfid.menu_rfid_config" action="rfid_device_config_action"
              sequence="30" groups="xq_rfid.group_rfid_manager"/>
</odoo>
